# DevContainer Environment Overrides
#
# Copy this file to .env in the same directory:
#   cp .devcontainer/.env.example .devcontainer/.env
#
# For Colima users: set DEVCONTAINER_HOST to your Colima VM IP.
# Find it with: colima list (look at the ADDRESS column)
# Common default: 192.168.64.2
#
# For Docker Desktop users: leave as localhost (default).

DEVCONTAINER_HOST=localhost
