; DevContainer PHP configuration
; Errors go to the Apache log, not into the HTTP response body
; (prevents PHP notices/warnings from corrupting JSON API responses)

display_errors = Off
log_errors = On
error_log = /var/log/apache2/php_error.log
error_reporting = E_ALL
